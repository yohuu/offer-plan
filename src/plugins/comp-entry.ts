import {
    // create naive ui
    create,
    // component
    NButton,
    NTable,
    NDatePicker,
    NInput
} from 'naive-ui'

export default create({
    components: [NButton, NTable, NDatePicker, NInput]
})
